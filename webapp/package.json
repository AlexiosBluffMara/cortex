{
  "name": "cortex-webapp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Cortex web viewer — Three.js cortex heatmap + Vite dev server",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "three": "^0.176.0"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  }
}
