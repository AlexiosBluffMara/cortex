# Cloudflare Pages redirects.
# Format: <source> <destination> <status>
# Docs: https://developers.cloudflare.com/pages/configuration/redirects/
#
# NOTE: Cloudflare Pages handles Clean URLs natively — cortex.html is auto-served
# at /cortex without any redirect rule here. The old .html rewrites were creating
# 308-loop conflicts with CF Pages' built-in Clean URLs behavior.

# Point /demo to the live Tailscale-fronted Cortex instance on Seratonin.
/demo     https://big-apple.scylla-betta.ts.net     302
